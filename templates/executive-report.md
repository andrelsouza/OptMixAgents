# Marketing Mix Model — Executive Report

> Generated by OptMix Reporter Agent (Nora Lindqvist)
> Date: {{ generated_date }}

---

## Executive Summary

{{ executive_summary }}

---

## Model Overview

| Metric | Value |
|--------|-------|
| **Model Type** | {{ model_type }} |
| **Time Period** | {{ date_range }} |
| **Observations** | {{ n_observations }} |
| **Target Variable** | {{ target_variable }} |
| **Channels Modeled** | {{ n_channels }} |
| **Model Fit (R²)** | {{ r_squared }} |
| **MAPE** | {{ mape }}% |

---

## Channel Performance

### ROAS by Channel

| Channel | Total Spend | Contribution | ROAS | Share of Effect |
|---------|------------|--------------|------|-----------------|
{% for ch in channels %}| {{ ch.name }} | ${{ ch.spend }} | ${{ ch.contribution }} | {{ ch.roas }}x | {{ ch.share }}% |
{% endfor %}

### Key Findings

{{ channel_findings }}

---

## Saturation Analysis

{{ saturation_analysis }}

**Channels approaching saturation:**
{% for ch in saturated_channels %}
- **{{ ch.name }}**: {{ ch.saturation_pct }}% saturated — {{ ch.recommendation }}
{% endfor %}

**Channels with room to grow:**
{% for ch in growth_channels %}
- **{{ ch.name }}**: {{ ch.saturation_pct }}% saturated — {{ ch.recommendation }}
{% endfor %}

---

## Budget Optimization

### Current vs. Optimal Allocation

| Channel | Current | Optimal | Change | Marginal ROAS |
|---------|---------|---------|--------|---------------|
{% for ch in allocation %}| {{ ch.name }} | ${{ ch.current }} | ${{ ch.optimal }} | {{ ch.change }} | {{ ch.mroas }}x |
{% endfor %}

**Total Budget:** ${{ total_budget }}
**Expected Lift:** {{ expected_lift }}
**Confidence Interval:** {{ confidence_interval }}

---

## Scenario Analysis

{% for scenario in scenarios %}
### {{ scenario.name }}

{{ scenario.description }}

**Impact:** {{ scenario.impact }}

{% endfor %}

---

## Recommendations

{{ recommendations }}

### Priority Actions

{% for action in priority_actions %}
{{ loop.index }}. **{{ action.title }}** — {{ action.description }}
   - *Impact:* {{ action.impact }}
   - *Effort:* {{ action.effort }}
   - *Timeline:* {{ action.timeline }}
{% endfor %}

---

## Methodology Notes

- **Adstock Model**: {{ adstock_type }} decay (captures advertising carryover effects)
- **Saturation Model**: {{ saturation_type }} function (captures diminishing returns)
- **Estimation**: {{ estimation_method }}
- **Validation**: {{ validation_approach }}

---

*Report generated by OptMix v{{ optmix_version }}*
